<FormRadioGroup
  @label={{@label}}
  @onChange={{@onToggle}}
  class="rounded-xl p-4 {{this.bgColor}}"
  as |Radio|
>
  <Radio @value="" @label="none" />
  {{#each @options as |option|}}
    <Radio @value={{option}} @label={{option}} />
  {{/each}}
</FormRadioGroup>